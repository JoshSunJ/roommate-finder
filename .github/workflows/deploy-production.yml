name: Deploy production

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  release:
    uses: ./.github/workflows/release-cloud-run.yml
    with:
      environment: production
